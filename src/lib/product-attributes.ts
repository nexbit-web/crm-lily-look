/**
 * Характеристики товару — рядки таблиці «Склад», «Посадка»… у картці на сайті.
 *
 * Назви не захардкоджені в базі: ProductAttribute зберігає пару «назва +
 * значення». Але фактично магазин заповнює всім товарам один і той самий
 * набір, тому CRM підставляє його в новий товар — щоб «Склад» не став десь
 * «складом», а десь «Складом, %».
 */
export const DEFAULT_ATTRIBUTE_NAMES = ['Склад', 'Посадка', 'Сезон', 'Країна виробництва'] as const;

/** Назва характеристики та значення, які вже зустрічаються в базі. */
export type AttributeOption = { name: string; values: string[] };
